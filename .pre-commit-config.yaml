repos:
  - repo: local
    hooks:
      - id: validate-frontmatter
        name: Validate entity frontmatter
        description: Проверяет frontmatter записей в entities/ по схемам категорий
        entry: python scripts/validate.py
        language: system
        files: ^entities/
